/**
 * Constantes para serem passadas na criação dos testes unitários, para que o teste seja
 * bem descritivo.
 */
/** Constante indicando que o componente foi criado */
export const TESTCOMPONENT: string = '(C) -> O componente de teste foi criado.';

/** Constante indicando que o serviço foi criado */
export const TESTSERVICE: string = '(S) -> O serviço de teste foi criado.';

/** Constante indicando que as propriedades do componente foram inicializadas */
export const PROPTAG: string =
  '(P) -> As propriedades do componente de teste foram inicializadas e definidas.';

/** Constante que indica que é o teste de uma variável */
export const VARTAG: string = '(V) ->';

/** Constante que indica que é o teste de um método */
export const METHODTAG: string = '(M) ->';

/** Constante que indica que passou pelo next do subscribe */
export const SUBNEXT: string = '(NEXT) ->';

/** Constante que indica que passou pelo error do subscribe */
export const SUBERROR: string = '(ERROR) ->';
